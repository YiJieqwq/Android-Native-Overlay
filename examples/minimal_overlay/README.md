# Minimal overlay example

This placeholder marks the intended home for a small, generic example. It will
use the reusable `template/` runtime without Linuxbkr-specific simulation logs,
branding, or audio.
